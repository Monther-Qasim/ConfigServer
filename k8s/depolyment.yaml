server:
  port: 9296
spring:
  application:
    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MontherQasim/Configration
          clone-on-start: true